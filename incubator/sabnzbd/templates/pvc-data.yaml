{{- if .Values.sabnzbd.persistence.data.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "sabnzbd.fullname" . }}-data
  labels:
    {{- include "sabnzbd.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.sabnzbd.persistence.data.accessMode }}
  {{- if .Values.sabnzbd.persistence.data.storageClass }}
  storageClassName: {{ .Values.sabnzbd.persistence.data.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.sabnzbd.persistence.data.size }}
{{- end }}