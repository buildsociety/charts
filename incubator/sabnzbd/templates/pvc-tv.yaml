{{- if .Values.sabnzbd.persistence.tv.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "sabnzbd.fullname" . }}-tv
  labels:
    {{- include "sabnzbd.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.sabnzbd.persistence.tv.accessMode }}
  {{- if .Values.sabnzbd.persistence.tv.storageClass }}
  storageClassName: {{ .Values.sabnzbd.persistence.tv.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.sabnzbd.persistence.tv.size }}
{{- end }}